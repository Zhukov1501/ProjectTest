<Window x:Class="TravelAgencyApp.Views.ClientEditWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Клиент" Height="550" Width="450"
        WindowStartupLocation="CenterScreen" ResizeMode="NoResize"
        Background="{StaticResource BackgroundBrush}">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock x:Name="txtTitle" Grid.Row="0" Text="Добавить клиента" 
                   FontSize="18" FontWeight="SemiBold" Margin="0,0,0,20"/>

        <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto">
            <StackPanel>
                <TextBlock Text="Фамилия *" FontSize="12" Margin="0,0,0,5"/>
                <TextBox x:Name="txtLastName" Style="{StaticResource ModernTextBox}" Margin="0,0,0,10"/>

                <TextBlock Text="Имя *" FontSize="12" Margin="0,0,0,5"/>
                <TextBox x:Name="txtFirstName" Style="{StaticResource ModernTextBox}" Margin="0,0,0,10"/>

                <TextBlock Text="Отчество" FontSize="12" Margin="0,0,0,5"/>
                <TextBox x:Name="txtMiddleName" Style="{StaticResource ModernTextBox}" Margin="0,0,0,10"/>

                <TextBlock Text="Дата рождения" FontSize="12" Margin="0,0,0,5"/>
                <DatePicker x:Name="dpBirthDate" Margin="0,0,0,10" FontSize="14"/>

                <TextBlock Text="Номер паспорта" FontSize="12" Margin="0,0,0,5"/>
                <TextBox x:Name="txtPassport" Style="{StaticResource ModernTextBox}" Margin="0,0,0,10"/>

                <TextBlock Text="Телефон" FontSize="12" Margin="0,0,0,5"/>
                <TextBox x:Name="txtPhone" Style="{StaticResource ModernTextBox}" Margin="0,0,0,10"/>

                <TextBlock Text="Email" FontSize="12" Margin="0,0,0,5"/>
                <TextBox x:Name="txtEmail" Style="{StaticResource ModernTextBox}" Margin="0,0,0,10"/>

                <TextBlock Text="Адрес" FontSize="12" Margin="0,0,0,5"/>
                <TextBox x:Name="txtAddress" Style="{StaticResource ModernTextBox}" Margin="0,0,0,10"/>

                <TextBlock Text="Заметки" FontSize="12" Margin="0,0,0,5"/>
                <TextBox x:Name="txtNotes" Style="{StaticResource ModernTextBox}" 
                         AcceptsReturn="True" Height="80" TextWrapping="Wrap"
                         VerticalScrollBarVisibility="Auto"/>
            </StackPanel>
        </ScrollViewer>

        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,20,0,0">
            <Button x:Name="btnDelete" Content="Удалить" Visibility="Collapsed"
                    Style="{StaticResource SecondaryButton}" Foreground="{StaticResource ErrorBrush}"
                    BorderBrush="{StaticResource ErrorBrush}" Margin="0,0,10,0"
                    Click="btnDelete_Click"/>
            <Button Content="Отмена" Style="{StaticResource SecondaryButton}" 
                    Margin="0,0,10,0" Click="btnCancel_Click"/>
            <Button Content="Сохранить" Style="{StaticResource PrimaryButton}" 
                    Click="btnSave_Click"/>
        </StackPanel>
    </Grid>
</Window>
