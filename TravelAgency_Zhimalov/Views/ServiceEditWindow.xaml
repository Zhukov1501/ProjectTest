<Window x:Class="TravelAgencyApp.Views.ServiceEditWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Услуга" Height="600" Width="500"
        WindowStartupLocation="CenterScreen" ResizeMode="NoResize"
        Background="{StaticResource BackgroundBrush}">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock x:Name="txtTitle" Grid.Row="0" Text="Добавить услугу" 
                   FontSize="18" FontWeight="SemiBold" Margin="0,0,0,20"/>

        <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto">
            <StackPanel>
                <TextBlock Text="Название *" FontSize="12" Margin="0,0,0,5"/>
                <TextBox x:Name="txtName" Style="{StaticResource ModernTextBox}" Margin="0,0,0,10"/>

                <TextBlock Text="Описание" FontSize="12" Margin="0,0,0,5"/>
                <TextBox x:Name="txtDescription" Style="{StaticResource ModernTextBox}" 
                         AcceptsReturn="True" Height="80" TextWrapping="Wrap" Margin="0,0,0,10"/>

                <TextBlock Text="Категория *" FontSize="12" Margin="0,0,0,5"/>
                <ComboBox x:Name="cmbCategory" IsEditable="True" Margin="0,0,0,10" FontSize="14">
                    <ComboBoxItem Content="Развлечения"/>
                    <ComboBoxItem Content="Экстрим"/>
                    <ComboBoxItem Content="Семейные"/>
                    <ComboBoxItem Content="Детские"/>
                    <ComboBoxItem Content="Технологии"/>
                    <ComboBoxItem Content="Водные"/>
                    <ComboBoxItem Content="Кино"/>
                    <ComboBoxItem Content="Спорт"/>
                    <ComboBoxItem Content="Комплекс"/>
                </ComboBox>

                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>

                    <StackPanel Grid.Column="0" Margin="0,0,5,0">
                        <TextBlock Text="Цена (₽) *" FontSize="12" Margin="0,0,0,5"/>
                        <TextBox x:Name="txtPrice" Style="{StaticResource ModernTextBox}" Margin="0,0,0,10"/>
                    </StackPanel>

                    <StackPanel Grid.Column="1" Margin="5,0,0,0">
                        <TextBlock Text="Длительность (мин)" FontSize="12" Margin="0,0,0,5"/>
                        <TextBox x:Name="txtDuration" Style="{StaticResource ModernTextBox}" Margin="0,0,0,10"/>
                    </StackPanel>
                </Grid>

                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>

                    <StackPanel Grid.Column="0" Margin="0,0,5,0">
                        <TextBlock Text="Макс. участников" FontSize="12" Margin="0,0,0,5"/>
                        <TextBox x:Name="txtMaxParticipants" Style="{StaticResource ModernTextBox}" Margin="0,0,0,10"/>
                    </StackPanel>

                    <StackPanel Grid.Column="1" Margin="5,0,0,0">
                        <TextBlock Text="Доступных мест" FontSize="12" Margin="0,0,0,5"/>
                        <TextBox x:Name="txtAvailableSlots" Style="{StaticResource ModernTextBox}" Margin="0,0,0,10"/>
                    </StackPanel>
                </Grid>

                <TextBlock Text="Локация" FontSize="12" Margin="0,0,0,5"/>
                <TextBox x:Name="txtLocation" Style="{StaticResource ModernTextBox}" Margin="0,0,0,10"/>

                <TextBlock Text="Возрастное ограничение" FontSize="12" Margin="0,0,0,5"/>
                <ComboBox x:Name="cmbAgeRestriction" Margin="0,0,0,10" FontSize="14">
                    <ComboBoxItem Content="0+" IsSelected="True"/>
                    <ComboBoxItem Content="3+"/>
                    <ComboBoxItem Content="4+"/>
                    <ComboBoxItem Content="5+"/>
                    <ComboBoxItem Content="6+"/>
                    <ComboBoxItem Content="8+"/>
                    <ComboBoxItem Content="12+"/>
                    <ComboBoxItem Content="16+"/>
                    <ComboBoxItem Content="18+"/>
                </ComboBox>

                <CheckBox x:Name="chkIsAvailable" Content="Доступно" IsChecked="True" Margin="0,10,0,0"/>
            </StackPanel>
        </ScrollViewer>

        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,20,0,0">
            <Button x:Name="btnDelete" Content="Удалить" Visibility="Collapsed"
                    Style="{StaticResource SecondaryButton}" Foreground="{StaticResource ErrorBrush}"
                    BorderBrush="{StaticResource ErrorBrush}" Margin="0,0,10,0"
                    Click="btnDelete_Click"/>
            <Button Content="Отмена" Style="{StaticResource SecondaryButton}" 
                    Margin="0,0,10,0" Click="btnCancel_Click"/>
            <Button Content="Сохранить" Style="{StaticResource PrimaryButton}" 
                    Click="btnSave_Click"/>
        </StackPanel>
    </Grid>
</Window>
